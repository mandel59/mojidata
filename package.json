{
    "name": "mojidata-workspace",
    "version": "1.4.1",
    "private": true,
    "scripts": {
        "test": "(cd packages/mojidata && yarn test) && (cd packages/idstool && yarn test) && (cd packages/mojidata-cli && yarn test)",
        "build": "(cd packages/mojidata && yarn prepare) && (cd packages/idstool && yarn prepare) && (cd packages/mojidata-cli && yarn prepare)"
    },
    "workspaces": {
        "packages": [
            "packages/*"
        ]
    },
    "devDependencies": {
        "@types/node": "^16.9.1",
        "typescript": "^4.4.3"
    },
    "packageManager": "yarn@4.2.2"
}
